# 下一步片段选择与路线建议

状态：`next_route_plan_completed_pending_user_decision`

## 主结论

`已确认`：当前不建议继续用上一轮 Wan-S2V `image_url + audio_url` 路线承诺“人物身体大幅移动复刻”。如果继续生成，只能定位为轻动作自然度 probe，不能定位为 1:1 motion reconstruction。

## 如果用户仍要做一次最小复测

- `source_01`：如果只做上肢/上身轻动作最小复测，可选 `50.0-65.0s`；它比 0-15s 更适合看手臂位置变化，但仍不是严格大幅身体移动。
- `source_02`：不建议作为大幅身体移动复测样本；它主要是桥式姿态保持。
- `source_03`：0-15s 已基本代表主体动作；重选收益有限。

## 如果目标是大幅身体移动

`通用建议`：用户需要补充真正大动作样本，例如站起/坐下、转身、侧移、走近/远离镜头、弯腰、全身重心变化明显的 10-15s 片段。

## 路线建议

1. 先确认供应商/API 是否支持 `video_url`、`motion_reference`、`pose_reference` 或等价字段。
2. 如果没有这些字段，停止把 Wan-S2V 当前路线当动作复刻路线。
3. 如果找到支持 motion reference 的路线，再用 `source_01` 高动作段做一次最小验证。
4. 所有新结果继续标 `待验证`，不得写人审通过或供应商验收通过。
