# V0 运行测试报告

## 总结

- V0 全链路运行：`FAIL`
- 本地底座单元测试：`PASS（7/7）`
- 占位启动脚本：`PASS as placeholder / FAIL as V0`
- 外部付费 API：`未调用`
- 源代码修改：`no`

## 执行命令与结果

### 1. 本地 tests

```text
PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s tests -v
```

结果：`Ran 7 tests ... OK`。覆盖配置字段、secret mask 和 mock 的 `QwenClient` 请求/错误处理。测试文件与 `src/live_brain/qwen_client.py` 均为本地 ignored，不在 GitHub `main`。

局限：HTTP 请求被 mock；不覆盖评论输入、知识库、规则、转人工、TTS、数字人或日志集成。

### 2. 占位启动脚本

```text
PYTHONDONTWRITEBYTECODE=1 python3 -B scripts/run_live_demo.py
```

结果摘要：

```text
AI live demo placeholder
.env exists: yes
Live push URL configured: no
Real live streaming is not implemented in this scaffold.
No stream push, OBS control, digital human logic, or paid API call.
```

该脚本按其占位目的运行成功，但作为 V0 启动入口判定 `FAIL`。

## V0 最低闭环测试矩阵

| 测试项 | 输入 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| 历史评论回放 | 至少 3 条不同评论 | 每条有唯一 event_id | 无回放入口 | FAIL |
| 分类 | 咨询/异议/促销/高风险 | 稳定类别与置信度 | 无分类器 | FAIL |
| 知识调用 | 经复核课程问题 | 返回引用 source_id | 无 runtime KB | FAIL |
| 动态回复 | 不同评论 | 非固定模板、受规则约束 | 仅 one-shot 底座/历史探针 | PARTIAL |
| 禁限表达 | 高风险/违规输入 | fail-closed 或转人工 | 无检查器 | FAIL |
| 人工接管 | 未知/风险/模型失败 | `manual_takeover` 事件 | 无队列/状态机 | FAIL |
| 阿里 3D 播报 | 安全回复 | 可见/可听播报和回执 | 仅离线视频样片 | FAIL |
| 全链路日志 | 任一评论 | 输入至播报全事件日志 | 只有模型探针报告 | FAIL |
| 可重复性 | 连续运行两次 | 结果结构一致、事件不丢失 | 无完整链路 | FAIL |

## 精确阻断原因

1. 没有标准化历史评论输入/回放入口。
2. 没有可执行分类、规则、知识检索和人工接管编排。
3. 没有阿里 3D 数字人动态播报适配器与回执。
4. 没有全链路事件日志。
5. 当前启动脚本明确是 placeholder。
6. GitHub `main` 缺本地候选代码、依赖清单、测试与 CI，不能 clean-clone 复现。

## 外部调用边界

本轮没有调用 Qwen、Wan、VideoRetalk、TTS、3D 数字人或转录付费 API。历史报告只能作为既有技术证据，不能替代本轮 V0 集成验证。
